apiVersion: v1
kind: Service
metadata:
  name: go-web-app
spec:
  type: NodePort
  selector:
    app: go-web-app
  ports:
    - port: 80           # Cluster IP port
      targetPort: 8080   # Container port
      nodePort: 30080    # External port you access from browser

